odoo.define('hms_image_zoom.product_variant', function (require) {

var ajax = require('web.ajax');

    $(document).ready(function() {
        $('.hms_image_preview').each(function() {
            var oe_website_sale = this;
            $("#footer").remove();
            $("header").remove();
        });
    });
});